#include "catch.hpp"
#include <iostream>
#include "NPC.h"
#include "Protagonist.h"

TEST_CASE("npc test", "[npc][test]") {
    // Protagonist m_player("player1", "张三");
    // NPC professor("导员", 1001);
    // professor.loadInteractionConfig(NPCType::PROFESSOR, "tests/unit/NPCs.json");
    // professor.startInteraction();

    // NPC student("学生", 1002);
    // student.loadInteractionConfig(NPCType::STUDENT, "tests/unit/NPCs.json");
    // student.startInteraction();

    // NPC DormManager("宿管", 1003);
    // DormManager.loadInteractionConfig(NPCType::DORM_MANAGER, "tests/unit/NPCs.json");
    // DormManager.startInteraction();

    // NPC roommate("舍友", 1004);
    // roommate.loadInteractionConfig(NPCType::ROOMMATE, "tests/unit/NPCs.json");
    // roommate.startInteraction();

    // NPC Employer("招聘人员", 1005);
    // Employer.loadInteractionConfig(NPCType::EMPLOYER, "tests/unit/NPCs.json");
    // Employer.startInteraction();
    
    // NPC Librarian("图书管理员", 1006);
    // Librarian.loadInteractionConfig(NPCType::LIBRARIAN, "tests/unit/NPCs.json");
    // Librarian.startInteraction();

    // NPC CanteenStaff("食堂工作人员", 1007);
    // CanteenStaff.loadInteractionConfig(NPCType::CANTEEN_STAFF, "tests/unit/NPCs.json");
    // CanteenStaff.startInteraction();

    // NPC Professor("教授", 1008);
    // Professor.loadInteractionConfig(NPCType::PROFESSOR, "tests/unit/NPCs.json");
    // Professor.startInteraction();

    // NPC Coach("教练", "",1009);
    // Coach.loadInteractionConfig(NPCType::COACH, ".config/NPCs.json");
    // Coach.startInteraction();

    // // Important!!! 这种表达式必须使用一个大括号括起来
    // REQUIRE((SeniorStudent.getTitle() == "aaa"));
    // REQUIRE((SeniorStudent.getInteractionText() == "你好,我是computer专业的学长aaa,最近有点忙,能帮帮我吗?"));

    // SECTION("modifing the student title") {
    //     SeniorStudent.setTitle("李四");
    //     REQUIRE(SeniorStudent.getTitle() == "李四");
    // }

    // // std::cout << SeniorStudent.getInteractionText() << std::endl;
    // SeniorStudent.startInteraction();
}
